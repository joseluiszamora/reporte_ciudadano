name: reporte_ciudadano
description: Prototipo ciudadano de consulta de problemas persistentes de El Alto.
publish_to: 'none'
version: 0.6.0+6

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
